def call(folderName,agentName,agentFileSystemPath,agentDescription,agentLabels,numOfExecutors)
{
    echo "Hello from SharedLibrary: agentName ${folderName}"
    echo "Hello from SharedLibrary: agentName ${agentName}"
    echo "Hello from SharedLibrary: agentFileSystemPath ${agentFileSystemPath}"
    echo "Hello from SharedLibrary: agentDescription ${agentDescription}"
    echo "Hello from SharedLibrary: agentLabels ${agentLabels}"
    echo "Hello from SharedLibrary: numOfExecutors ${numOfExecutors}"

    echo "Agent has been created successfully."
}